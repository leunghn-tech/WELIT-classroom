import { Minus, Plus, UsersRound } from 'lucide-react';

export default function TeamScoreboard({ teams, activeIndex, onActivate, onAdjust, teamCount, onTeamCount }) {
  return <section className="classroom-scoreboard" aria-label="全班隊伍記分板"><header><span><UsersRound size={16} /> 全班隊伍賽</span><div role="group" aria-label="隊伍數量">{[2, 3, 4].map((count) => <button key={count} className={teamCount === count ? 'active' : ''} onClick={() => onTeamCount(count)}>{count} 隊</button>)}</div></header><p>輪到 <b style={{ color: teams[activeIndex]?.color }}>{teams[activeIndex]?.name}</b> 作答；答對自動得分，老師可按需要加減分。</p><div className="classroom-team-grid">{teams.map((team, index) => <article key={team.name} className={index === activeIndex ? 'active' : ''} style={{ '--team-color': team.color }}><button className="team-select" onClick={() => onActivate(index)} aria-pressed={index === activeIndex}><span>{index === activeIndex ? '本回合' : `第 ${index + 1} 隊`}</span><b>{team.name}</b></button><div><button onClick={() => onAdjust(index, -1)} aria-label={`扣除 ${team.name} 一分`}><Minus size={14} /></button><strong>{team.score}</strong><button onClick={() => onAdjust(index, 1)} aria-label={`增加 ${team.name} 一分`}><Plus size={14} /></button></div><small>連勝 {team.streak}</small></article>)}</div></section>;
}
